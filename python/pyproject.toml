[project]
name = "agentic-commerce-research-tools"
version = "0.1.0"
description = "Prototype Python tooling for agentic commerce reliability experiments."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

